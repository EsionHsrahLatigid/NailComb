name: CI

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:
    inputs:
      force_heavy:
        description: Run heavy plugin builds for manual dispatch
        required: false
        type: boolean
        default: true

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: EsionHsrahLatigid/juce-ci/.github/workflows/plugin-ci.yml@926c5bb10335c29503f92869169759ec18d71449
    permissions:
      contents: read
    with:
      product_name: NailComb
      product_slug: nailcomb
      cmake_option_prefix: NAILCOMB
      debug_targets_json: '["nailcomb_dsp_tests"]'
      release_test_targets_json: '["nailcomb_dsp_tests","nailcomb_plugin_tests","nailcomb_editor_tests"]'
      windows_debug_plugins: false
